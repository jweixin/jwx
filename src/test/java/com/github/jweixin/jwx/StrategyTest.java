package com.github.jweixin.jwx;

public class StrategyTest {
	/*
	@Test
	public void testGetMsgStrategy() throws ClassNotFoundException{
		Class<?> clazz = Class.forName("com.telecomjs.yc.jwx.TestClass");
		Method[] methods = clazz.getMethods();
		for(Method method : methods){
			if(method.isAnnotationPresent(TextMsg.class)){
				TextMsg anno = method.getAnnotation(TextMsg.class);
				Assert.assertNotNull(MsgStrategyFactory.getInstance().annotationStrategyCreator(anno.annotationType()));
			}
		}
		
	}*/

}
