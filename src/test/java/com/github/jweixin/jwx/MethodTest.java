package com.github.jweixin.jwx;

public class MethodTest {
	/*
	@Test
	public void testMethod() throws ClassNotFoundException{
		Class<?> clazz = Class.forName("com.telecomjs.yc.jwx.TestClass");
		Method[] methods = clazz.getMethods();
		for(Method method : methods){
			if(method.isAnnotationPresent(TestAnno.class)){
				TestAnno anno = method.getAnnotation(TestAnno.class);
				Assert.assertEquals(anno.value(), "test");
				Class<?> ret = method.getReturnType();
				Assert.assertEquals(ret, void.class);
			}
		}
	}
	
	@Test
	public void testAnno() throws ClassNotFoundException{
		Class<?> clazz = Class.forName("com.telecomjs.yc.jwx.TestClass");
		Method[] methods = clazz.getDeclaredMethods();
		for(Method method : methods){
			Assert.assertTrue(WeixinMethod.hasWeixinAnnotationType(method));
		}
	}*/
}
